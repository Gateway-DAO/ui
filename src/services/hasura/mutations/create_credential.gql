mutation create_cred($gatewayId: String!, $score: Int!, $bearerToken: String!) {
  create_cred(
    create_cred_input: {
      gatewayId: $gatewayId
      score: $score
      bearerToken: $bearerToken
    }
  ) {
    credentialId
  }
}
