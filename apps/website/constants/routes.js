const ROUTES = {
  LANDING: '/',
  EXPLORE: '/home',
  NEW_USER: '/new-user',
  CREDENTIALS: '/credentials',
  CREDENTIALS_NEW: '/credentials/new',
  DAO_PROFILE: '/dao/[id]',
  GATE_PROFILE: '/gate/[id]',
};

module.exports = {
  ROUTES,
};
