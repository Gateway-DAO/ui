const ROUTES = {
  LANDING: '/',
  EXPLORE: '/home',
  SEARCH: '/search/[key]',
  NEW_USER: '/new-user',
  CREDENTIALS: '/credentials',
  CREDENTIALS_NEW: '/credentials/new',
  DAO_PROFILE: '/dao/[id]',
  GATE_PROFILE: '/gate/[id]',
};

module.exports = {
  ROUTES,
};
