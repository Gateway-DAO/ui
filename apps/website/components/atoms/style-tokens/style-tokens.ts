import { ResponsiveStyleValue } from '@mui/system';

/**
 * Reusable style tokens for responsiveness acorss the website.
 */

export const CONTAINER_PX: ResponsiveStyleValue<number> = [2, 4, null, 6];
