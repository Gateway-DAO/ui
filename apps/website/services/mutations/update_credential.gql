mutation complete_credential(
  $group_id: uuid!
  $details: jsonb
  $accomplishments: jsonb
) {
  update_credentials_by_pk(
    pk_columns: { id: $group_id }
    _set: { status: "pending", details: $details, pow: $accomplishments }
  ) {
    id
    dao_id
    name
  }
}
