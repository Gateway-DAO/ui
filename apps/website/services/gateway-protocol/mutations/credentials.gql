mutation createCredential(
  $dataModelId: String!,
  $title: String!
  $description: String!
  $expirationDate: DateTime
  $claim: JSON
  $image: String
  $issuerOrganizationId: String
  $recipientUserGatewayIdOrWallet: String!
  $tags: [String!]!
) {
  createCredential(
    createCredentialInput: {
      dataModelId: $dataModelId
      title: $title
      description: $description
      expirationDate: $expirationDate
      claim: $claim
      image: $image
      issuerOrganizationId: $issuerOrganizationId
      recipientUserGatewayIdOrWallet: $recipientUserGatewayIdOrWallet
      tags: $tags
    }) {
    id
  }
}

mutation updateCredential(
  $id: String!
  $title: String
  $description: String
  $claim: JSON
  $image: String
) {
  updateCredential(updateCredentialInput: {
    id: $id
    title: $title
    description: $description
    claim: $claim
    image: $image
  }) {
    id
    claim
  }
}

mutation revokeCredential($id: String!) {
  revokeCredential(id: $id) {
    status
  }
}
