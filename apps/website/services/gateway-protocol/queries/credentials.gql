query credentials {
  credential {
    _id
  }
}

query credential($id: String!) {
  credentialById(id:$id) {
    _id
    evidences
    claim
    dataModel {
      _id
      title
      tags
    }

    issuer {
      primaryWallet {
        address
        ens
      }
    }
    recipient {
      primaryWallet {
        address
        ens
      }
    }

    issuanceDate
    expirationDate

    tags

    title
    description

    createdAt
		updatedAt
  }
}
