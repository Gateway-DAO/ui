query dataModel($id: String!) {
  dataModel(id: $id) {
    id
    description
    schema
    tags
    title
    arweaveInfo
    createdAt
  }
}

query dataModels($take: Int!, $skip: Int!) {
  dataModels(take: $take, skip: $skip) {
    id
    title
    tags
    version
    description
  }
}
